# Team Storage Paths

Team config:

`~/.claude/teams/{team-name}/config.json`

Task list:

`~/.claude/tasks/{team-name}/`

Notes:
- team config is runtime state and gets overwritten
- do not hand-edit it
- use subagent definitions for reusable roles instead of trying to author team config by hand
